我审计了最新 `main`，当前提交是 [`335fcf6`](https://github.com/NarcissismT/PESCO/commit/335fcf6)。结论是：

> **P2.3.3-r1 的“策略归因修复”已经通过，可以正式关闭；但整个 P2.3.3 仍是 NO-GO。下一步应做 P2.3.4：解决强基线、family 泛化和 estimator validity，而不是运行 private promotion-v5，也不是扩大到 1.5B–3B。**

仓库自己的最终 [gate.json](https://github.com/NarcissismT/PESCO/blob/335fcf6/artifacts/tier1_p233_r1_final_atomic_target/gate.json) 也正确写成了：

```json
"status": "NO_GO",
"diagnostic_only": true,
"private_data_accessed": false
```

这不是失败，而是很重要的阶段性分界：

* r1 证明“PESCO 的动作策略确实在训练”；
* r1 证明 Branch 和 Flip 在 Full 内有机制贡献；
* 但尚未证明 PESCO 优于最强非 Full 方法、严格 shortcut baseline，或能跨机制族泛化。

## 一、这次哪些问题确实解决了

### 1. RF adapter 归因污染已消除

新的纯因子训练入口 `fit_rollout_method_authentic_factorial`：

* 所有方法从同一个 SFT checkpoint 开始；
* action head 不再冻结；
* 没有 RandomForest 覆盖 logits；
* 相同 optimizer、learning rate、batch schedule、1024-step budget；
* 仅通过 `use_state/use_branch/use_flip` 切换因子；
* 所有 cell 共享 Atomic reward。

对应实现见 [`tier1_p231_diagnostics.py`](https://github.com/NarcissismT/PESCO/blob/335fcf6/research_strategy_optimization/evaluation/tier1_p231_diagnostics.py)。

10 个 seed 中，PESCO-Full 均满足：

* action-head parameter delta > 0；
* 训练后 action logits 改变；
* 无外部 adapter；
* selected action 实际发生改变。

因此可以确认：

> 当前 Full 的结果来自神经策略本身，而不是上一版的 RF 替代策略。

### 2. Branch 在 Full 中获得了独立贡献证据

关键对比：

$$
R_{\text{Full}}-R_{\text{Atomic+State+Flip}}
=-0.01915
$$

95% CI：

$$
[-0.02715,-0.01142]
$$

同时 Branch 的全因子主效应为：

$$
-0.00895,\qquad
95\%CI=[-0.01352,-0.00436]
$$

这两个 CI 上界都小于 0。因此，在当前 diagnostic ID 分布和当前表格环境中，可以支持：

> 加入 Branch objective 能降低 regret，而且不是只由某一个因子 cell 或 RF adapter 造成的。

这是比上一版实质性更强的结果。

### 3. Flip 在 Full 内保留下来了

现在比较的是正确的：

$$
\mathrm{PairRank}(\text{Full})
-
\mathrm{PairRank}(\text{Atomic+State+Branch})
$$

结果为：

$$
+0.01440,\qquad
95\%CI=[+0.01025,+0.01848]
$$

而对应 regret：

$$
-0.00276,\qquad
95\%CI=[-0.00814,+0.00123]
$$

满足预注册的 0.01 非劣界。

因此可以支持：

> Flip 不只是在 `Atomic+Flip` 中有效，在完整 PESCO 中也保留了偏好反转贡献，同时没有明显损害 regret。

### 4. 收敛问题基本修复

1024-step 下：

* Full plateau gate 通过；
* tail regret slope 约为 \(-3.6\times10^{-6}\)；
* 10 seeds 全部执行；
* 使用共同训练预算；
* 没有每方法事后选择 checkpoint。

因此上一版“64 步结果却用 1024 步收敛文件背书”的问题已经解决。

## 二、为什么现在仍不能进入 promotion-v5

剩余三项 false 不是无关紧要的附加指标，而是回答了比 r1 更高一层的问题。

### 1. Full 没有显著击败最强非 Full 方法

当前：

| 方法                       | Promotion regret |
| ------------------------ | ---------------: |
| PESCO-Full               |         0.153442 |
| FullInfo-ExpectedUtility |         0.154053 |
| Atomic+State+Branch      |         0.156204 |
| Atomic+Branch            |         0.158986 |
| GRPO-Atomic              |         0.161534 |
| Atomic+State+Flip        |         0.172595 |

Full 相对 tune 冻结的最强非 Full baseline：

$$
\Delta R=-0.000611
$$

但 95% CI 为：

$$
[-0.004873,+0.002892]
$$

因此只能说“点估计略好且非劣”，不能说“显著优于”。

这里不建议通过增加 seed 或修改阈值来追求显著性。效应本身只有约 \(6.1\times10^{-4}\)，即使 CI 收窄，实际意义也很弱。

### 2. Full 仍输给严格 RF shortcut baseline

严格、不使用 confirmation 特征的 RF：

$$
R_{\mathrm{RF}}=0.14533
$$

而：

$$
R_{\mathrm{Full}}=0.15344
$$

差距约为：

$$
+0.00811
$$

这说明当前问题存在两种可能：

1. PESCO 的 MLP/policy optimization 还没有充分利用公开观测特征；
2. 当前数据生成器仍有容易被树模型捕获的规则性结构。

因此，现阶段不能宣称“新的 Branch/Flip RL 方法整体优于强监督策略”。当前能支持的更窄结论是：

> 在统一神经策略和 Atomic reward 下，Branch 与 Flip loss 具有正的因子贡献。

### 3. family 泛化失败，而且比“2/10”更严重

ID promotion 中只有 2/10 family 上 Full 优于 Atomic+State+Flip。

更值得注意的是 [`family_loo.json`](https://github.com/NarcissismT/PESCO/blob/335fcf6/artifacts/tier1_p233_r1_final_atomic_target/family_loo.json)：

* 部分 family 中 Full 确实改善；
* 多个 family 中 Full 输给 Atomic+State+Flip；
* `heterogeneous_noise` 和 `group_generalization` 的 LOO 指标为 `null`；
* 但 `ten_family_runs_completed=true` 仍让 `leave_one_family_out_robust` 通过。

所以当前这个门禁只证明“脚本运行了十次”，并没有证明 LOO robustness。

下一版必须把它改成性能门禁，例如：

```yaml
loo_all_families_have_finite_metrics: true
loo_full_noninferior_in_at_least_8_of_10_families: true
loo_macro_regret_ci_upper_lt_zero: true
loo_worst_family_regret_noninferior: true
```

## 三、还有两个没有体现在那三项 false 里的问题

### 1. Measurement-shift estimator 实际完全失效

[`coverage_calibration.json`](https://github.com/NarcissismT/PESCO/blob/335fcf6/artifacts/tier1_p233_r1_final_atomic_target/coverage_calibration.json) 中：

| 机制                  |        Bias | 95% coverage |
| ------------------- | ----------: | -----------: |
| heterogeneous noise |     +0.0018 |        0.940 |
| nonlinear response  |     +0.0002 |        0.942 |
| measurement shift   | **−0.3616** |    **0.000** |
| MNAR stress test    |     −0.0042 |        0.944 |
| noncompliance       |     −0.0029 |        0.958 |

这说明：

* “estimator 不再读取 hidden truth”已经通过；
* 但“estimator 是有效的”没有通过。

当前 gate 只检查 source audit 和 leakage，没有给每个机制设置 bias/RMSE/coverage 门槛。因此它把“无 oracle 泄漏”和“统计有效性”混成了一个门禁。

P2.3.4 应拆开：

```yaml
estimator_integrity:
  no_hidden_truth_access: true
  counterfactual_leakage_pass: true

estimator_validity:
  all_mechanisms_finite: true
  per_mechanism_bias_within_bound: true
  per_mechanism_coverage_lower_bound_ge_090: true
  measurement_shift_calibration_pass: true
```

Measurement shift 需要真实的 calibration/anchor 数据或误差模型；不能仅继续增加 bootstrap 次数。

### 2. promotion-v5 runner 默认仍绑定旧产物

当前 [`run_tier1_p233_promotion_v5.py`](https://github.com/NarcissismT/PESCO/blob/335fcf6/scripts/run_tier1_p233_promotion_v5.py) 默认引用的仍是：

```text
artifacts/tier1_p233_matrix_10seed/...
artifacts/tier1_p233_diagnostic/...
```

而不是新的：

```text
artifacts/tier1_p233_r1_final_atomic_target/...
```

更根本的问题是，它绑定的仍是 diagnostic dataset，而不是一个真正独立、尚未打开的 private ID/OOD dataset commitment。

所以即使以后 gate 变为 GO，也不能直接运行这个 runner。

## 四、下一步建议：P2.3.4

P2.3.4 不需要重做已经通过的 r1 主矩阵。应把预算集中在三条尚未建立的能力上。

### 工作包 A：强基线差距诊断

固定当前数据和评测，进行一次低成本归因：

* `PESCO-Full-MLP`；
* 相同输入的监督 MLP；
* RF；
* GBDT；
* FullInfo-ExpectedUtility；
* PESCO-Full 加容量匹配；
* PESCO-Full 加与 RF 相同的可见特征，但保持端到端神经策略。

报告：

* train/tune/promotion regret；
* action imitation error；
* calibration；
* 按 family 的 regret；
* RF 与 Full 选错动作的交叉表；
* 在 RF 正确、Full 错误的样本上，差异来自哪类特征或状态。

决策规则：

* 如果容量匹配的监督 MLP 仍输 RF，问题主要是表示/优化；
* 如果监督 MLP 能追平 RF，而 PESCO 输，问题主要是 RL objective；
* 如果所有神经方法都输 RF，数据可能过度表格化，不适合作为进入 LLM 的核心证据。

### 工作包 B：真正的 family-held-out 泛化

对十个 family 重新执行严格 LOO：

```text
排除 family F 的全部 train/tune 数据
→ 重新训练 SFT
→ 重新训练所有关键方法
→ 仅在全新的 F 上评测
```

至少保留：

* SFT；
* RF/GBDT；
* Atomic+State+Branch；
* Atomic+State+Flip；
* PESCO-Full。

不要再只检查“十次运行完成”。建议晋级门槛：

```yaml
all_loo_metrics_finite: true
full_vs_asf_loo_macro_regret_ci_upper_lt_zero: true
full_vs_asb_loo_pairrank_ci_lower_gt_zero: true
full_noninferior_in_at_least_8_of_10_families: true
worst_family_regret_noninferior: true
```

如果 Full 仍只在少数 family 获益，应将论文主张限制为：

> Branch/Flip 在已知机制分布内改善策略学习。

而不是：

> 学会了可跨未知科研机制泛化的实验推理。

### 工作包 C：修复 estimator validity

优先修 `measurement_shift`：

* 引入独立 calibration set 或 anchor measurement；
* estimator 只能读取观测数组和 calibration receipt；
* calibration 样本预算必须计入环境成本；
* 每个 simulated dataset 独立形成 CI；
* 500–1000 个外层重复；
* 分别报告 repaired/unrepaired bias、RMSE 和 coverage。

MNAR 继续明确称为 stress test，除非提供 selection model、shadow variable 或 sensitivity bound；否则不要声称点识别。

## 五、P2.3.4 的推荐总门禁

```yaml
p234_go:
  p233_r1_policy_authenticity_pass: true
  branch_factorial_effect_ci_upper_lt_zero: true
  full_vs_asf_regret_ci_upper_lt_zero: true
  full_vs_asb_pairrank_ci_lower_gt_zero: true

  full_vs_best_frozen_nonfull_regret_ci_upper_lt_zero: true
  full_beats_strict_rf_gbdt: true
  practical_effect_size_floor_pass: true

  all_loo_metrics_finite: true
  family_majority_direction_positive: true
  loo_macro_regret_ci_upper_lt_zero: true
  worst_family_noninferiority_pass: true

  no_hidden_truth_used_by_estimator: true
  all_estimator_bias_bounds_pass: true
  all_estimator_coverage_lower_bounds_pass: true
  measurement_shift_calibration_pass: true

  private_dataset_commitment_created_after_code_freeze: true
  promotion_runner_bound_to_r1_checkpoints: true
  one_shot_private_access_guard_pass: true
```

这里建议加入 `practical_effect_size_floor_pass`。否则可能通过增加样本数，把 \(-0.0006\) 这种几乎没有实际意义的差异变成统计显著。

## 六、什么时候进入 private promotion 和 1.5B–3B

推荐顺序是：

```text
P2.3.3-r1：归因真实性
已完成
        ↓
P2.3.4：强基线 + family LOO + estimator validity
当前下一步
        ↓
Promotion-v6：真正独立的 private ID + whole-family OOD
尚不可运行
        ↓
1.5B–3B：raw evidence → state → action
仅在 private promotion 通过后
```

进入 1.5B–3B 前还需要重新冻结：

* base checkpoint/revision；
* tokenizer；
* SFT checkpoint；
* 训练和推理 token budget；
* question manifest；
* verifier；
* container/dependency digest；
* confirmation access policy；
* contamination audit。

我本地在不安装全部可选依赖的环境运行标准测试得到 **106 passed / 19 skipped / 0 failed**；与你报告的 **126/1** 差异来自依赖环境和测试发现范围，但没有发现失败测试。

最终阶段定位是：

> **P2.3.3-r1 已成功证明 Branch、Flip 和动作策略更新的真实性；现在的核心瓶颈已从“归因是否可信”转移到“是否优于强基线、是否跨 family 泛化、统计 repair 是否有效”。因此下一版只做 P2.3.4，不访问 private promotion，不进入 LLM。**
