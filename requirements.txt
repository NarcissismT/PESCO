# Core Tier-0/reference loop uses only the Python standard library.
# Tier-1 NumPy environment and PNG/SVG visualization require:
numpy>=1.23
matplotlib>=3.6
# PyTorch is optional; objective/loss tests use it when available.
