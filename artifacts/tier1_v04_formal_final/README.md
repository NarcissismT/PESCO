# Tier-1 v0.4 formal environment (historical consumed artifact)

> **Status: `diagnostic_v04_consumed` (2026-08-26).**  The environment and
> whole-family holdout receipts are retained for provenance and execution audit,
> but the latent-template overlap review means this is not an independent clean
> final.  Formal model comparison remains locked.

See [`consumption_notice.json`](consumption_notice.json) for the machine-readable
consumption boundary.  The original JSON files and `run_manifest.json` digest are
unchanged; the notice is additive and does not rewrite historical evidence.

The successor boundary is the fresh P2.1 diagnostic in
[`../tier1_p21_diagnostic/`](../tier1_p21_diagnostic/) and the prepared v0.5
public/private evaluator split in [`../tier1_v05_frozen_final/`](../tier1_v05_frozen_final/).
