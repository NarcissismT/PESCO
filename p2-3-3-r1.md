我审计了最新 `main`，当前提交是 [`8bfde78`](https://github.com/NarcissismT/PESCO/commit/8bfde782fe511e1a1b488cbeab194c7a87883ccd)。本地测试结果为 106 项完成、19 项跳过、无失败。

结论非常明确：

> 这版确实修掉了 OOD estimator 直接读取 `latent` 的问题，也正确废止了 promotion‑v4；但是当前 `P2.3.3 GO` 不能成立。`PESCO-Full` 的最终动作主要由额外的 RandomForest adapter 决定，而不是 Branch/Flip 梯度学习。下一步应做 P2.3.3-r1“策略归因修正版”，不要授权或访问 promotion‑v5。

## 当前阶段判断

| 部分                            | 判断            |
| ----------------------------- | ------------- |
| OOD estimator 不再直接读取 latent   | 基本 GO         |
| 数据规模、10-seed、paired bootstrap | 有明显进步         |
| Flip 单独机制                     | GO            |
| Branch 单独机制                   | 表面改善，但因子归因不干净 |
| PESCO-Full 策略学习               | NO-GO         |
| 当前 P2.3.3 gate                | 应撤销           |
| promotion-v5                  | NO-GO，保持未访问   |
| LLM/QLoRA/GSPO                | NO-GO         |

## 为什么当前漂亮结果不能解释为 PESCO 成立

当前 promotion 结果确实很好看：[P2.3.3 aggregate](https://github.com/NarcissismT/PESCO/blob/8bfde782fe511e1a1b488cbeab194c7a87883ccd/artifacts/tier1_p233_matrix_10seed/p233_aggregate_promotion.json)

| 方法                 |     Regret | PairRank |
| ------------------ | ---------: | -------: |
| GRPO-Atomic        |     0.2122 |    0.665 |
| Atomic+Flip        |     0.1542 |    0.821 |
| Atomic+State+Flip  |     0.1566 |    0.926 |
| Atomic+Branch+Flip |     0.1393 |    0.671 |
| PESCO-Full         | **0.1362** |    0.676 |

Full 相对最强非 Full 方法的 regret 差值为：

$$
-0.00309,\quad 95\%CI=[-0.00643,-0.00024]
$$

但这个结果存在决定性的归因问题。

### 1. PESCO-Full 的动作网络实际上被冻结了

在 [`tier1_p231_diagnostics.py`](https://github.com/NarcissismT/PESCO/blob/8bfde782fe511e1a1b488cbeab194c7a87883ccd/research_strategy_optimization/evaluation/tier1_p231_diagnostics.py) 中，Full 在更新前冻结了：

* trunk；
* action head；
* flip head；
* belief head；
* state-to-action 层。

随后虽然仍然执行 optimizer step，但主要更新的是独立 state representation；Branch 和 Flip 无法更新最终动作头。

代码甚至加入了零值 autograd anchor，让日志中仍然出现“完成优化器更新”，但这不等于动作策略被更新。

### 2. 评测时用 RandomForest 覆盖了动作输出

PESCO-Full 和 Atomic+Branch+Flip 会额外训练 RandomForest，标签是：

```text
argmax_atomic_branch_receipt
```

也就是 evaluator 给出的四分支最佳动作。

评测时，[evaluation code](https://github.com/NarcissismT/PESCO/blob/8bfde782fe511e1a1b488cbeab194c7a87883ccd/research_strategy_optimization/evaluation/tier1_differentiable_suite.py) 不再使用神经网络的 action logits，而是：

```text
RandomForest.predict → 直接覆盖 action logits
```

因此当前 `PESCO-Full` 更接近：

> SFT state calibrator + 监督训练的 RandomForest action classifier。

而不是：

> 通过 Branch credit 和 Flip loss 更新得到的 PESCO 策略。

这也解释了为什么 Full 看似击败了 standalone RF：实际上是在比较不同参数的 RF，而不是 PESCO 对比 RF。

### 3. 收敛结果证明动作策略没有在学习

Full 在不同训练步数下的 tune regret 完全一致：

|   步数 |  Full regret |
| ---: | -----------: |
|   64 | 0.1351936609 |
|  128 | 0.1351936609 |
|  256 | 0.1351936609 |
|  512 | 0.1351936609 |
| 1024 | 0.1351936609 |

这不是理想收敛，而是因为 action head 被冻结、最终动作又被 RF 接管。[收敛产物](https://github.com/NarcissismT/PESCO/blob/8bfde782fe511e1a1b488cbeab194c7a87883ccd/artifacts/tier1_p233_convergence/convergence_summary.json)

同时：

* convergence gate 声称冻结了 1024 步；
* 10-seed matrix 的 [run manifest](https://github.com/NarcissismT/PESCO/blob/8bfde782fe511e1a1b488cbeab194c7a87883ccd/artifacts/tier1_p233_matrix_10seed/run_manifest.json) 实际写的是 `finetune_steps=64`。

所以当前 convergence gate 与被评测矩阵的训练配置没有绑定。

### 4. Full 没有保留 Flip 能力

真正应该检查的是：

$$
\mathrm{PairRank}(Full)
-
\mathrm{PairRank}(Atomic+State+Branch)
$$

或者至少：

$$
\mathrm{PairRank}(Full)
-
\mathrm{PairRank}(Atomic+State+Flip)
$$

但当前 gate 检查的是：

```text
Atomic+Flip vs GRPO-Atomic
```

所以即使 Full 的 Flip 能力失败，gate 仍然可以通过。

实际结果：

$$
0.676-0.926=-0.249
$$

95% CI 为：

$$
[-0.303,-0.200]
$$

即 Full 的 PairRank 明显低于 Atomic+State+Flip。当前 [P2.3.3 gate](https://github.com/NarcissismT/PESCO/blob/8bfde782fe511e1a1b488cbeab194c7a87883ccd/artifacts/tier1_p233_matrix_10seed/p233_gate.json) 对 PESCO-Full 的 Flip 贡献检查错位了。

### 5. 八格实验不再是纯因子实验

Full 与其消融方法之间不只相差 Branch/State/Flip 开关，还同时改变了：

* 是否使用 RandomForest；
* 是否冻结 action head；
* learning rate；
* full-information objective；
* utility-target CE；
* state loss weight；
* pairwise loss weight；
* state-conditioning scale；
* checkpoint selection rule；
* branch normalization。

因此：

```text
Full − Atomic+State+Flip
```

不能再解释为 Branch 的独立贡献。

同理：

```text
Atomic+Branch+Flip − Atomic+Flip
```

还混入了 RF adapter 和不同 checkpoint 规则。

## 评测器仍有四个问题

### OOD estimator：直接 latent 泄漏已修复，但 coverage 尚不可信

新的 estimator 确实只接收观测数组，这是实质改进。[Estimator 实现](https://github.com/NarcissismT/PESCO/blob/8bfde782fe511e1a1b488cbeab194c7a87883ccd/research_strategy_optimization/environments/tier1_tabular_env.py)

但当前 audit 仍有问题：

* “hidden truth mutation”只设置了两个没有参与计算的局部变量，然后把同一函数调用两次；
* coverage 使用跨 100 个数据集的全局标准差，不是每个数据集自身估计的标准误；
* MNAR coverage 实际使用随机缺失的 MCAR mask，不是真正 MNAR；
* gate 只检查是否生成 coverage receipt，没有检查每类 estimator 是否达到 coverage/bias 阈值。

因此这里应判断为：

> 直接 oracle 已修复，但 estimator validity 尚未完成。

### Counterfactual leakage test 是形式测试

当前脚本修改了局部 metadata 副本，但之后仍对原始 observation 重新编码，所以 feature hash 必然相同。[泄漏审计脚本](https://github.com/NarcissismT/PESCO/blob/8bfde782fe511e1a1b488cbeab194c7a87883ccd/scripts/audit_tier1_p233_counterfactual_leakage.py)

正确测试应当：

1. 重新生成同一个世界；
2. 改变未选择分支的隐藏效果或 confirmation；
3. 重新执行 decision-time observation collection；
4. 比较完整 observation、MLP features 和 RF features 的 hash。

### Reward stability 是数学恒真的

当前只把完整 atomic utility 同时乘以：

```text
0.8 / 1.0 / 1.2
```

正数整体缩放不会改变 argmax，因此 winner stability 必然是 100%。[稳定性脚本](https://github.com/NarcissismT/PESCO/blob/8bfde782fe511e1a1b488cbeab194c7a87883ccd/scripts/run_tier1_p233_reward_stability.py)

应分别扰动：

* validity；
* replication；
* scientific utility；
* cost penalty；
* confirmation bonus；
* discovery utility。

并检查方法排名和非平局动作是否稳定。

### 当前 promotion 不是机制 OOD

新数据规模很好：

* 1,000 train questions；
* 400 tune；
* 400 promotion；
* 10 mechanism families；
* 386 promotion reversal clusters。

但每个 split 都包含相同十个机制族，并使用同一个 `_params` 生成公式，只是 variant 区间不同。[数据生成器](https://github.com/NarcissismT/PESCO/blob/8bfde782fe511e1a1b488cbeab194c7a87883ccd/research_strategy_optimization/evaluation/tier1_p233_dataset.py)

因此它应称为：

```text
large-scale diagnostic ID promotion
```

不能称为 whole-family OOD。

## 下一步：P2.3.3-r1

### 第一步：立即撤销当前 GO

建议新增：

```json
{
  "status": "INVALIDATED_DIAGNOSTIC_BEFORE_PRIVATE_ACCESS",
  "reasons": [
    "PESCO-Full action head frozen",
    "RandomForest overrides action logits",
    "factorial cells change multiple components",
    "Full flip contribution gate mis-specified",
    "64-step matrix not bound to 1024-step convergence"
  ],
  "private_data_accessed": false
}
```

不要运行 promotion‑v5。

### 第二步：先做一个廉价归因实验

比较：

| 方法                               | 目的                  |
| -------------------------------- | ------------------- |
| RF-only                          | 当前 adapter 的真实能力    |
| SFT + RF adapter                 | 判断 Full 是否等于 SFT+RF |
| PESCO-Full，关闭 RF                 | 检查神经策略真实性能          |
| PESCO-Full，Branch weight=0       | Branch 因果贡献         |
| PESCO-Full，Flip weight=0         | Flip 因果贡献           |
| PESCO-Full，冻结前后 action-head hash | 确认动作参数真实变化          |

必须分别记录：

```text
trunk parameter delta
action-head parameter delta
flip-head parameter delta
state-head parameter delta
action-logit change
selected-action change
```

当前只记录整个模型 digest，state head 变化就能掩盖 action head 完全未变。

### 第三步：恢复真正的八格因子矩阵

所有八个方法必须共享：

* 完全相同的模型结构；
* 完全相同的初始化；
* learning rate；
* optimizer；
* batch schedule；
* checkpoint selection；
* state-conditioning rule；
* utility-target rule；
* 训练步数；
* 无 sklearn adapter。

唯一允许不同的是：

```text
use_state
use_branch
use_flip
```

RandomForest 可以保留，但必须作为独立方法：

```text
RF-ReceiptPolicy
```

不能嵌入 PESCO-Full。

### 第四步：修正关键门禁

新的机制门禁应是：

```yaml
branch:
  full_vs_atomic_state_flip_regret_ci_upper_lt_zero: true
  factorial_branch_main_effect_ci_upper_lt_zero: true

flip:
  full_vs_atomic_state_branch_pairrank_ci_lower_gt_zero: true
  full_vs_atomic_state_branch_regret_noninferior: true

state:
  full_vs_atomic_branch_flip_state_f1_noninferior: true
  min_per_state_recall_pass: true

policy_authenticity:
  full_action_head_parameter_delta_positive: true
  full_action_logits_change_after_training: true
  no_external_adapter_overrides_action_logits: true
```

尤其要补上：

> Flip 必须在 Full 内产生贡献，而不仅是 Atomic+Flip 单独有效。

### 第五步：重新纳入完整基线

当前公开 aggregate 仍然排除了 SFT。下一版必须包括：

* SFT；
* Logistic Regression；
* Random Forest；
* GBDT；
* RLOO-Atomic；
* GRPO-Atomic；
* GRPO-Stratified-4；
* FullInfo-ExpectedUtility；
* 八格 State/Branch/Flip；
* Oracle branch search，仅作上界。

最强 baseline 必须在 tune 上冻结选择，然后在 promotion 上比较；不能在 promotion 上重新选择最优 shortcut。

目前不用增加 GSPO。当前仍是一步四分类 CPU contextual bandit，GSPO 的序列级优势没有可比对象。

### 第六步：真正修复 estimator audit

* 使用 AST/source dataflow 检查 estimator 不引用 `WorldSpec.true_effect_*`、`latent`；
* 每个模拟数据集独立估计标准误或 bootstrap CI；
* 在独立重复数据集上计算 coverage；
* MNAR 要么增加 shadow variable/selection assumption，要么改称 MAR；
* 对每类机制预注册 bias、RMSE、coverage 下限；
* coverage 至少运行 500–1,000 replicates，而不是 100。

### 第七步：修 promotion‑v5 guard

当前 [promotion-v5 runner](https://github.com/NarcissismT/PESCO/blob/8bfde782fe511e1a1b488cbeab194c7a87883ccd/scripts/run_tier1_p233_promotion_v5.py) 仍有三个问题：

1. `AUTHORIZED` 只依赖 gate GO，没有同时要求 dataset/checkpoint/dependency digest 全部通过；
2. sentinel 使用 `os.replace`，会覆盖已有 sentinel，不是真正 one-shot；
3. 绑定的是 diagnostic dataset，不是未来 private promotion dataset commitment。

应改成：

```python
authorized = (
    gate_go
    and gate_digest_match
    and dataset_commitment_match
    and checkpoint_bundle_verified
    and evaluator_digest_match
    and config_digest_match
    and dependency_lock_present
)
```

sentinel 使用 `O_CREAT | O_EXCL`，已存在时立即拒绝。

## 重新运行顺序

```text
1. 3-seed attribution smoke test
2. 修纯因子矩阵
3. 3-seed 64/128/256/512/1024 convergence
4. 冻结一个共同训练预算
5. 10-seed diagnostic ID
6. 十个 leave-one-family-out 训练/评测
7. 修正后的 P2.3.3-r1 gate
8. 通过后重新创建 promotion-v5 commitment
9. 独立 private ID/OOD
10. 再进入 1.5B–3B LLM
```

最终判断是：

> 这次更新在环境规模和数据型 estimator 上有真实进展，但算法结果发生了“RF adapter 代替 PESCO 策略”的归因倒退。下一步不是继续扩大数据或进入 promotion，而是移除 adapter、解冻动作网络、恢复纯因子矩阵，并证明 Branch 与 Flip 都真实改变了 PESCO-Full 的动作参数和未见问题决策。
